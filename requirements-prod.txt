# 生产环境依赖（与新版 backend_api/requirements.txt + backend_core 最小运行时对齐）
# 不含开发/测试工具（pytest、black 等见 backend_api/requirements-dev.txt）。
# 修改 API 运行时依赖请编辑：backend_api/requirements.txt
# 修改 core 最小依赖请编辑：backend_core/requirements-minimal.txt

-r backend_api/requirements.txt
-r backend_core/requirements-minimal.txt
