# PVFRS量价频幅度共振策略选股实现逻辑文档

## 概述

PVFRS（Price, Volume, Frequency, Resonance, Strength）策略是一个基于量价频三维共振演化的股票选股系统。该策略通过分析价格、频率、成交量三个维度的指标，检测三维共振状态，从而识别具有高效率演化轨道的股票。

## 核心架构

### 1. 策略组件架构

```
StrategyEngine (策略引擎)
├── PriceDimensionAnalyzer (价格维度分析器)
├── FrequencyDimensionAnalyzer (频率维度分析器)  
├── VolumeDimensionAnalyzer (成交量维度分析器)
├── ResonanceDetector (三维共振检测器)
├── SignalGenerator (信号生成器)
├── EntryTimingOptimizer (入场时机优化器)
└── RiskManager (风险管理器)
```

### 2. 数据流程

```
市场数据 → 三维分析器 → 共振检测 → 信号生成 → 时机优化 → 风险管理 → 交易信号
```

## 详细实现逻辑

### 一、价格维度分析 (Price Dimension)

#### 1.1 核心指标计算

**宏观位移指标 (Macro Displacement)**
```
公式: Δ = d₂₀ - d₁
其中:
- d₁: 观察周期第1天收盘价
- d₂₀: 观察周期第20天收盘价
- Δ: 20天价格变化幅度
```

**即时强度指标 (Instant Deviation)**
```
公式: d₂₀ - d
其中:
- d: 20日平均价格 = (d₁ + d₂ + ... + d₂₀) / 20
- d₂₀ - d: 当前价格相对于均线的偏离程度
```

**20日平均价格 (Average Price)**
```
公式: d = (d₁ + d₂ + ... + d₂₀) / 20
用途: 作为价格波动的基准线
```

#### 1.2 辅助指标

**乖离率 (Bias)**
```
公式: BIAS = (当前价格 - 20日均价) / 20日均价
用途: 衡量价格偏离均线的程度
```

**价格波动率 (Price Volatility)**
```
计算方法: 20天内价格收益率的标准差
用途: 衡量价格波动剧烈程度
```

**趋势持续性 (Trend Persistence)**
```
分析方法: 计算价格趋势的连续性
用途: 判断趋势是否稳定
```

#### 1.3 价格维度条件验证

**基础条件:**
- 宏观位移 ≥ 配置最小值 (默认: 0)
- 即时强度 ≥ 配置最小值 (默认: 0)
- 趋势持续性满足要求
- 价格波动率在合理范围内

**增强条件:**
- 乖离率 ≥ 2% (默认配置)
- 相对位移 ≥ 5% (默认配置)
- 连续天数满足要求 (默认: 2天)

### 二、频率维度分析 (Frequency Dimension)

#### 2.1 核心指标计算

**上涨天数 (Rising Days)**
```
公式: Z = count(dᵢ > dᵢ₋₁) for i in [2, 20]
含义: 20天内上涨的天数
```

**下跌天数 (Falling Days)**
```
公式: F = count(dᵢ < dᵢ₋₁) for i in [2, 20]  
含义: 20天内下跌的天数
```

**频率优势 (Frequency Advantage)**
```
条件: Z > F
含义: 上涨天数严格多于下跌天数
```

#### 2.2 频率质量分析

**虚假繁荣检测 (False Prosperity)**
```
检测逻辑: 
- 如果上涨天数很多但涨幅很小
- 或者下跌天数很少但跌幅很大
- 则判定为虚假繁荣
```

**上涨集中度 (Rising Concentration)**
```
计算方法: 分析上涨天数的分布情况
用途: 判断上涨是否集中在特定时期
```

**近期持续性 (Recent Persistence)**
```
分析窗口: 最近5-10天
判断标准: 近期是否保持上涨趋势
```

#### 2.3 频率维度条件验证

**基础条件:**
- 频率优势: Z > F (必须满足)
- 无虚假繁荣现象
- 上涨集中度合理
- 近期持续性良好

**增强条件:**
- 连续买入支撑
- 充足的优势幅度
- 趋势稳定性

### 三、成交量维度分析 (Volume Dimension)

#### 3.1 核心指标计算

**20日平均成交量 (Average Volume)**
```
公式: m = (m₁ + m₂ + ... + m₂₀) / 20
其中: mᵢ 为第i天的成交量
```

**效率指标 (Efficiency Indicator)**
```
公式: m₂₀ - m
其中:
- m₂₀: 当日成交量
- m: 20日平均成交量
含义: 成交量相对于均线的偏离程度
```

**效率比 (Efficiency Ratio)**
```
公式: m₂₀ / m
含义: 当日成交量与平均成交量的比值
```

#### 3.2 量价共振分析

**量价共振检测**
```
检测逻辑:
1. 价格上涨 + 成交量放大 = 正向共振
2. 价格上涨 + 成交量萎缩 = 背离信号
3. 价格下跌 + 成交量放大 = 恐慌性抛售
4. 价格下跌 + 成交量萎缩 = 正常调整
```

**资金支撑质量 (Fund Support Quality)**
```
分析维度:
- 连续放量天数
- 放量倍数
- 资金流入持续性
- 成交量趋势稳定性
```

**成交量趋势持续性 (Volume Trend Persistence)**
```
分析方法:
- 计算成交量的移动平均
- 分析成交量变化趋势
- 判断资金关注度
```

#### 3.3 成交量维度条件验证

**基础条件:**
- 成交量效率 ≥ 配置最小值 (默认: 0)
- 量价共振状态良好
- 资金支撑质量合格

**增强条件:**
- 连续放量确认
- 高质量资金信号
- 成交量趋势持续性

### 四、三维共振检测 (Three-Dimensional Resonance)

#### 4.1 共振条件判定

**基础共振条件:**
```
价格维度有效 AND 频率维度有效 AND 成交量维度有效
```

**高效率演化轨道确认:**
```
条件:
1. 三维基础共振满足
2. 共振强度 ≥ 阈值
3. 各维度协同性良好
4. 无明显冲突信号
```

#### 4.2 共振强度计算

**条件权重配置:**
```
价格维度权重: 40%
频率维度权重: 30%  
成交量维度权重: 30%
```

**共振强度公式:**
```
Resonance_Strength = Σ(条件权重 × 条件满足度)
其中:
- 条件满足度 = 实际满足条件数 / 总条件数
- 共振强度范围: 0-1
```

**详细条件权重:**
```python
condition_weights = {
    'price_dimension_valid': 0.15,      # 价格维度基础
    'frequency_advantage': 0.20,       # 频率优势（核心）
    'volume_price_resonance': 0.25,     # 量价共振（核心）
    'volume_efficiency': 0.10,          # 成交量效率
    'bias_quality': 0.10,               # 乖离率质量
    'trend_persistence': 0.10,          # 趋势持续性
    'fund_support_quality': 0.10       # 资金支撑质量
}
```

### 五、信号强度计算 (Signal Strength)

#### 5.1 基础信号强度

**计算公式:**
```
Base_Strength = 共振强度 × 维度协同系数
```

**维度协同系数:**
```
基于各维度指标的一致性程度计算:
- 高度协同: 1.0-1.2
- 中度协同: 0.8-1.0  
- 低度协同: 0.6-0.8
```

#### 5.2 质量调整

**信号质量等级:**
```
高质量 (High Quality): 强度 ≥ 0.8
中等质量 (Medium Quality): 0.6 ≤ 强度 < 0.8
低质量 (Low Quality): 强度 < 0.6
```

**乖离率质量得分:**
```
计算逻辑:
- 乖离率在合理范围内加分
- 乖离率趋势良好加分
- 乖离率过度偏离扣分
```

#### 5.3 最终信号强度

**调整公式:**
```
Final_Strength = Base_Strength × 质量系数 × 乖离率系数
```

**强度阈值:**
```
最小信号强度: 0.6 (默认配置)
高质量信号阈值: 0.8 (默认配置)
```

### 六、入场时机优化 (Entry Timing Optimization)

#### 6.1 价格穿越检测

**穿越条件:**
```
当前价格 > 20日均线 + 缓冲区
其中:
- 缓冲区 = 20日均线 × 0.001 (0.1%)
```

**穿越强度分析:**
```
计算方法:
- 穿越幅度 = (当前价格 - 均线) / 均线
- 穿越趋势 = 近期价格走势
- 穿越持续性 = 连续穿越天数
```

#### 6.2 成交量突破确认

**突破条件:**
```
当日成交量 > 20日平均成交量 × 1.2
```

**突破质量分析:**
```
评估维度:
- 突破倍数
- 连续突破天数
- 资金流入强度
- 成交量趋势
```

#### 6.3 幅度校验

**幅度系数计算:**
```
公式: Δ₂₀ / d
其中:
- Δ₂₀: 宏观位移
- d: 20日平均价格
```

**校验范围:**
```
最小幅度: 1% (默认配置)
最大幅度: 30% (默认配置)
```

### 七、投资建议生成 (Investment Recommendations)

#### 7.1 信号等级分类

**强烈买入 (Strong Buy):**
```
条件:
- 信号强度 ≥ 0.8
- 三维共振完美
- 入场时机优化通过
- 风险评估低
```

**买入 (Buy):**
```
条件:
- 0.6 ≤ 信号强度 < 0.8
- 三维共振满足
- 部分时机优化通过
- 风险评估中等
```

**观望 (Hold):**
```
条件:
- 信号强度 < 0.6
- 部分维度共振
- 时机不成熟
- 风险评估较高
```

#### 7.2 投资建议内容

**建议要素:**
```
1. 信号等级和强度
2. 共振状态描述
3. 入场时机分析
4. 风险提示
5. 仓位建议
6. 持有期预期
```

### 八、共振状态评估 (Resonance Status)

#### 8.1 共振状态分类

**完美共振 (Perfect Resonance):**
```
特征:
- 三维条件全部满足
- 共振强度 ≥ 0.9
- 各维度高度协同
- 无冲突信号
```

**良好共振 (Good Resonance):**
```
特征:
- 三维条件基本满足
- 共振强度 0.7-0.9
- 维度间协同良好
- 轻微冲突信号
```

**弱共振 (Weak Resonance):**
```
特征:
- 部分维度条件满足
- 共振强度 0.5-0.7
- 维度间协同一般
- 存在冲突信号
```

**无共振 (No Resonance):**
```
特征:
- 维度条件不满足
- 共振强度 < 0.5
- 维度间冲突明显
- 不具备投资价值
```

#### 8.2 共振持续性预测

**持续性评估指标:**
```
1. 趋势稳定性
2. 资金关注度
3. 市场环境适应性
4. 技术面支撑强度
```

**持续性预测模型:**
```
输入: 当前共振状态 + 历史表现 + 市场环境
输出: 持续性概率 + 预期持续时间
```

### 九、风险控制机制 (Risk Control)

#### 9.1 止损机制

**固定止损:**
```
默认: -6%
触发条件: 当前价格 ≤ 入场价格 × (1 - 6%)
```

**动态止损:**
```
移动止损: 盈利15%后启用
止损幅度: 最高价回撤10%
```

#### 9.2 止盈机制

**固定止盈:**
```
默认: +25%
触发条件: 当前价格 ≥ 入场价格 × (1 + 25%)
```

**分批止盈:**
```
第一阶段: +15% 止盈50%
第二阶段: +25% 止盈剩余
```

#### 9.3 时间管理

**最大持有期:**
```
默认: 45天
特殊情况: 可根据共振状态调整
```

**趋势反转检测:**
```
检测条件:
- 价格跌破重要均线
- 成交量异常放大
- 共振状态恶化
- 技术指标背离
```

### 十、配置参数说明

#### 10.1 买入条件参数

```python
# 基础PVFRS条件
'buy_macro_displacement_min': 0,      # 宏观位移最小值
'buy_instant_deviation_min': 0,       # 即时强度最小值  
'buy_rising_days_advantage': True,    # 上涨天数优势
'buy_efficiency_min': 0,              # 效率指标最小值

# 增强买入条件
'buy_bias_min': 0.02,                 # 最小偏离度2%
'buy_relative_displacement_min': 0.05, # 最小相对位移5%
'buy_consecutive_days': 2,            # 连续确认天数
'buy_price_above_ma5': False,         # 价格必须在5日均线之上
'buy_ma5_above_ma20': False,          # 5日均线必须在20日均线之上
```

#### 10.2 卖出条件参数

```python
# 基础卖出条件
'sell_below_ma20': True,              # 收盘价跌破20日均线卖出
'stop_loss': -0.06,                   # 止损-6%
'take_profit': 0.25,                  # 止盈25%
'max_holding_days': 45,               # 最大持有天数

# 风险管理参数
'sell_bias_max': 0.15,                # 最大偏离度15%
'sell_instant_deviation_max': 0.10,   # 最大即时强度10%
'sell_divergence_days': 3,            # 价涨量缩背离天数
```

#### 10.3 风险管理参数

```python
# 动态风险管理
'profit_stage1': 0.15,                # 盈利阶段1: 15%
'sell_reversal_conditions_low_profit': 3,  # 低盈利时需要的反转条件数
'sell_reversal_conditions_high_profit': 2, # 高盈利时需要的反转条件数

# 仓位管理
'max_position_size': 0.1,             # 最大仓位10%
```

## 总结

PVFRS策略通过多维度分析实现了对股票的全面评估：

1. **价格维度**：评估价格趋势和偏离程度
2. **频率维度**：分析涨跌频率和趋势质量  
3. **成交量维度**：检测资金流向和量价关系
4. **共振检测**：整合三维分析结果，识别高效率演化轨道
5. **信号生成**：基于共振强度和质量生成交易信号
6. **时机优化**：选择最佳入场时机
7. **风险控制**：动态管理投资风险

该策略的核心优势在于通过三维共振检测，能够有效识别具有持续上涨潜力的股票，同时通过严格的风险控制机制，确保投资安全。

---

**文档版本**: 1.0  
**更新时间**: 2025-06-27  
**适用版本**: PVFRS策略完整实现版
