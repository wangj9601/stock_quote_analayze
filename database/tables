-- Table: public.historical_quotes_hk

-- DROP TABLE IF EXISTS public.historical_quotes_hk;

CREATE TABLE IF NOT EXISTS public.historical_quotes_hk
(
    code text COLLATE pg_catalog."default" NOT NULL,
    ts_code text COLLATE pg_catalog."default",
    name text COLLATE pg_catalog."default",
    date text COLLATE pg_catalog."default" NOT NULL,
    open real,
    high real,
    low real,
    close real,
    pre_close real,
    volume real,
    amount real,
    amplitude real,
    turnover_rate real,
    change_percent real,
    change real,
    five_day_change_percent real,
    ten_day_change_percent real,
    sixty_day_change_percent real,
    thirty_day_change_percent real,
    collected_source text COLLATE pg_catalog."default",
    collected_date timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    english_name text COLLATE pg_catalog."default",
    change_amount real,
    create_date timestamp without time zone,
    CONSTRAINT historical_quotes_hk_pkey PRIMARY KEY (code, date)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.historical_quotes_hk
    OWNER to postgres;
-- Index: idx_historical_quotes_hk_code

-- DROP INDEX IF EXISTS public.idx_historical_quotes_hk_code;

CREATE INDEX IF NOT EXISTS idx_historical_quotes_hk_code
    ON public.historical_quotes_hk USING btree
    (code COLLATE pg_catalog."default" ASC NULLS LAST)
    TABLESPACE pg_default;
-- Index: idx_historical_quotes_hk_collected_date

-- DROP INDEX IF EXISTS public.idx_historical_quotes_hk_collected_date;

CREATE INDEX IF NOT EXISTS idx_historical_quotes_hk_collected_date
    ON public.historical_quotes_hk USING btree
    (collected_date ASC NULLS LAST)
    TABLESPACE pg_default;
-- Index: idx_historical_quotes_hk_date

-- DROP INDEX IF EXISTS public.idx_historical_quotes_hk_date;

CREATE INDEX IF NOT EXISTS idx_historical_quotes_hk_date
    ON public.historical_quotes_hk USING btree
    (date COLLATE pg_catalog."default" ASC NULLS LAST)
    TABLESPACE pg_default;




    -- Table: public.ma_indicators

-- DROP TABLE IF EXISTS public.ma_indicators;

CREATE TABLE IF NOT EXISTS public.ma_indicators
(
    code character varying(20) COLLATE pg_catalog."default" NOT NULL,
    date character varying(20) COLLATE pg_catalog."default" NOT NULL,
    market_type character varying(10) COLLATE pg_catalog."default" NOT NULL,
    ma5 real,
    ma10 real,
    ma20 real,
    ma30 real,
    ma60 real,
    ma120 real,
    ma200 real,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT ma_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.ma_indicators
    OWNER to postgres;


-- Table: public.boll_indicators

-- DROP TABLE IF EXISTS public.boll_indicators;

CREATE TABLE IF NOT EXISTS public.boll_indicators
(
    code character varying(20) COLLATE pg_catalog."default" NOT NULL,
    date character varying(20) COLLATE pg_catalog."default" NOT NULL,
    market_type character varying(10) COLLATE pg_catalog."default" NOT NULL,
    mid real,
    upper real,
    lower real,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT boll_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.boll_indicators
    OWNER to postgres;
    
        
        -- Table: public.mavol_indicators

-- DROP TABLE IF EXISTS public.mavol_indicators;

CREATE TABLE IF NOT EXISTS public.mavol_indicators
(
    code character varying(20) COLLATE pg_catalog."default" NOT NULL,
    date character varying(20) COLLATE pg_catalog."default" NOT NULL,
    market_type character varying(10) COLLATE pg_catalog."default" NOT NULL,
    mavol5 real,
    mavol10 real,
    mavol20 real,
    mavol30 real,
    mavol60 real,
    mavol120 real,
    mavol200 real,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT mavol_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.mavol_indicators
    OWNER to postgres;


    -- Table: public.macd_indicators

-- DROP TABLE IF EXISTS public.macd_indicators;

CREATE TABLE IF NOT EXISTS public.macd_indicators
(
    code character varying(20) COLLATE pg_catalog."default" NOT NULL,
    date character varying(20) COLLATE pg_catalog."default" NOT NULL,
    market_type character varying(10) COLLATE pg_catalog."default" NOT NULL,
    dif real,
    dea real,
    macd real,
    ema12 real,
    ema26 real,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT macd_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.macd_indicators
    OWNER to postgres;


    -- Table: public.kdj_indicators

-- DROP TABLE IF EXISTS public.kdj_indicators;

CREATE TABLE IF NOT EXISTS public.kdj_indicators
(
    code character varying(20) COLLATE pg_catalog."default" NOT NULL,
    date character varying(20) COLLATE pg_catalog."default" NOT NULL,
    market_type character varying(10) COLLATE pg_catalog."default" NOT NULL,
    k real,
    d real,
    j real,
    rsv real,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT kdj_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.kdj_indicators
    OWNER to postgres;


    -- Table: public.rsi_indicators

-- DROP TABLE IF EXISTS public.rsi_indicators;

CREATE TABLE IF NOT EXISTS public.rsi_indicators
(
    code character varying(20) COLLATE pg_catalog."default" NOT NULL,
    date character varying(20) COLLATE pg_catalog."default" NOT NULL,
    market_type character varying(10) COLLATE pg_catalog."default" NOT NULL,
    rsi6 real,
    rsi12 real,
    rsi24 real,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT rsi_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.rsi_indicators
    OWNER to postgres;

    
-- Table: public.mean_frequency_resonance_indicators

-- DROP TABLE IF EXISTS public.mean_frequency_resonance_indicators;

CREATE TABLE IF NOT EXISTS public.mean_frequency_resonance_indicators
(
    code character varying COLLATE pg_catalog."default" NOT NULL,
    date character varying COLLATE pg_catalog."default" NOT NULL,
    market_type character varying COLLATE pg_catalog."default" NOT NULL,
    macro_displacement_delta double precision,
    instant_deviation double precision,
    rising_days_z integer,
    falling_days_f integer,
    efficiency_m20_minus_m double precision,
    ma20_d double precision,
    mavol20_m double precision,
    created_at timestamp without time zone,
    bias real,
    CONSTRAINT mean_frequency_resonance_indicators_pkey PRIMARY KEY (code, date, market_type)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.mean_frequency_resonance_indicators
    OWNER to postgres;    