# 港股历史数据采集 - 快速开始指南

## 🚀 5分钟快速上手

### 步骤1：选择采集程序

**我应该使用哪个程序？**

```
┌─────────────────────────────────────┐
│  是否已有实时行情数据？              │
└─────────────────────────────────────┘
           │
           ├─── 是 ──→ manual_hk_historical_collection.py
           │           （快速，适合日常同步）
           │
           └─── 否 ──→ direct_hk_historical_collection.py
                       （直接从API获取，适合初始化）
```

### 步骤2：配置参数

#### 方案A：使用 manual_hk_historical_collection.py

打开文件，找到配置区域（文件末尾）：

```python
# ========== 配置采集参数 ==========
START_DATE = '2024-01-01'      # 修改为你的开始日期
END_DATE = '2024-01-31'        # 修改为你的结束日期
COLLECTION_MODE = 'specified'  # 选择采集模式
STOCK_CODES = ['00700', '09988', '01810']  # 指定股票代码
```

#### 方案B：使用 direct_hk_historical_collection.py

打开文件，找到配置区域（文件末尾）：

```python
# ========== 配置采集参数 ==========
START_DATE = '2024-01-01'
END_DATE = '2024-01-31'
COLLECTION_MODE = 'specified'
CALCULATE_INDICATORS = True    # 是否计算技术指标
STOCK_CODES = ['00700', '09988', '01810']
```

### 步骤3：运行程序

```bash
# 方案A
python manual_hk_historical_collection.py

# 或方案B
python direct_hk_historical_collection.py
```

### 步骤4：确认并开始

程序会显示配置信息，输入 `y` 确认开始采集。

---

## 📋 常用场景示例

### 场景1：采集3只股票的最近1个月数据

**推荐程序：** `direct_hk_historical_collection.py`

```python
START_DATE = '2024-01-01'
END_DATE = '2024-01-31'
COLLECTION_MODE = 'specified'
CALCULATE_INDICATORS = True
STOCK_CODES = [
    '00700',  # 腾讯控股
    '09988',  # 阿里巴巴-SW
    '01810',  # 小米集团-W
]
```

**预计时间：** 约15秒（3只股票 × 5秒间隔）

---

### 场景2：采集所有港股的最近1周数据

**推荐程序：** `direct_hk_historical_collection.py`

```python
START_DATE = '2024-01-22'
END_DATE = '2024-01-26'
COLLECTION_MODE = 'all'
CALCULATE_INDICATORS = True
```

**预计时间：** 约3.8小时（2730只股票 × 5秒间隔）

⚠️ **注意：** 全量采集时间较长，建议在空闲时段运行

---

### 场景3：同步今天的自选股数据

**推荐程序：** `manual_hk_historical_collection.py`

**前提条件：** 已采集今天的实时行情数据

```python
from datetime import datetime
today = datetime.now().strftime('%Y-%m-%d')

START_DATE = today
END_DATE = today
COLLECTION_MODE = 'watchlist'
```

**预计时间：** 约1分钟

---

### 场景4：补充缺失的历史数据

**推荐程序：** `direct_hk_historical_collection.py`

```python
# 假设发现2024-01-15到2024-01-20的数据缺失
START_DATE = '2024-01-15'
END_DATE = '2024-01-20'
COLLECTION_MODE = 'specified'
CALCULATE_INDICATORS = True
STOCK_CODES = ['00700']  # 指定需要补充的股票
```

---

## ⚙️ 采集模式说明

### 模式1：指定股票 (specified)

```python
COLLECTION_MODE = 'specified'
STOCK_CODES = ['00700', '09988']
```

- ✅ 适合：测试、少量股票、特定股票
- ⏱️ 速度：快
- 💾 数据量：小

### 模式2：全量采集 (all)

```python
COLLECTION_MODE = 'all'
# 不需要指定 STOCK_CODES
```

- ✅ 适合：初始化数据库、批量更新
- ⏱️ 速度：慢（取决于股票数量）
- 💾 数据量：大

### 模式3：自选股 (watchlist)

```python
COLLECTION_MODE = 'watchlist'
# 不需要指定 STOCK_CODES
```

- ✅ 适合：日常更新、关注的股票
- ⏱️ 速度：中等
- 💾 数据量：中等
- ⚠️ 仅 manual 程序支持

---

## 📊 查看采集结果

### 方法1：查看日志文件

```bash
# manual 程序的日志
cat manual_hk_collection.log

# direct 程序的日志
cat direct_hk_collection.log
```

### 方法2：查询数据库

```sql
-- 查看采集的历史数据
SELECT date, COUNT(*) as stock_count
FROM historical_quotes_hk
WHERE date BETWEEN '2024-01-01' AND '2024-01-31'
GROUP BY date
ORDER BY date;

-- 查看特定股票的数据
SELECT *
FROM historical_quotes_hk
WHERE code = '00700'
  AND date BETWEEN '2024-01-01' AND '2024-01-31'
ORDER BY date;

-- 查看技术指标数据
SELECT date, dif, dea, macd
FROM macd_indicators
WHERE code = '00700'
  AND market_type = 'HK'
  AND date BETWEEN '2024-01-01' AND '2024-01-31'
ORDER BY date;
```

---

## ⚠️ 常见问题快速解决

### 问题1：提示"未找到港股实时行情表中的数据"

**原因：** 使用 manual 程序但没有实时数据

**解决：** 
- 方案1：先采集实时数据
- 方案2：改用 direct 程序

---

### 问题2：采集速度很慢

**原因：** direct 程序受API限流限制

**解决：**
- 这是正常现象，每只股票间隔5秒
- 可以分批采集，减少单次采集的股票数量
- 或改用 manual 程序（如果有实时数据）

---

### 问题3：股票代码无效

**原因：** 股票代码不存在于数据库中

**解决：**
```python
# 检查股票代码格式（必须是5位数字）
STOCK_CODES = ['00700']  # ✅ 正确
STOCK_CODES = ['700']    # ❌ 错误，需要补零
```

---

### 问题4：没有计算技术指标

**原因：** 
- manual 程序：股票不在自选股列表中
- direct 程序：`CALCULATE_INDICATORS = False`

**解决：**
- manual 程序：将股票添加到自选股
- direct 程序：设置 `CALCULATE_INDICATORS = True`

---

## 🎯 推荐工作流程

### 新项目初始化

```bash
# 1. 采集最近1年的历史数据（全量）
python direct_hk_historical_collection.py
# 配置：COLLECTION_MODE = 'all'
#      START_DATE = '2023-02-01'
#      END_DATE = '2024-01-31'

# 2. 设置定时任务，每日同步
# 使用 manual 程序 + cron/任务计划程序
```

### 日常维护

```bash
# 每天收盘后运行（推荐使用定时任务）
python manual_hk_historical_collection.py
# 配置：COLLECTION_MODE = 'watchlist'
#      START_DATE = 今天
#      END_DATE = 今天
```

### 数据补充

```bash
# 发现缺失数据时
python direct_hk_historical_collection.py
# 配置：COLLECTION_MODE = 'specified'
#      指定缺失的日期和股票代码
```

---

## 📝 检查清单

采集前请确认：

- [ ] 已安装所需依赖（akshare, pandas, sqlalchemy等）
- [ ] 数据库已初始化
- [ ] 配置参数已正确设置
- [ ] 日期格式正确（YYYY-MM-DD）
- [ ] 股票代码格式正确（5位数字）
- [ ] 网络连接正常（direct 程序）
- [ ] 已有实时数据（manual 程序）

---

## 🔧 高级技巧

### 技巧1：批量采集大量股票

```python
# 将股票列表分成多个批次
batch1 = ['00700', '09988', '01810']
batch2 = ['00175', '00388', '00939']
batch3 = ['01024', '01093', '01109']

# 分别运行，避免一次性采集时间过长
```

### 技巧2：只采集工作日

```python
from datetime import datetime, timedelta

def get_weekdays(start, end):
    """获取日期范围内的工作日"""
    current = datetime.strptime(start, '%Y-%m-%d')
    end_date = datetime.strptime(end, '%Y-%m-%d')
    weekdays = []
    
    while current <= end_date:
        if current.weekday() < 5:  # 周一到周五
            weekdays.append(current.strftime('%Y-%m-%d'))
        current += timedelta(days=1)
    
    return weekdays

# 使用
weekdays = get_weekdays('2024-01-01', '2024-01-31')
for day in weekdays:
    collect_data(start_date=day, end_date=day, ...)
```

### 技巧3：自动重试失败的股票

程序已内置错误处理，失败的股票会自动跳过并记录到日志。
查看日志文件找出失败的股票，然后单独重新采集。

---

## 📚 更多资源

- **详细文档：** `manual_hk_historical_collection_README.md`
- **程序对比：** `港股历史数据采集程序对比.md`
- **代码注释：** 查看程序源代码中的详细注释
- **工作流程：** `.agent/workflows/hk_data_collection_summary.md`

---

## 💡 小贴士

1. **首次使用建议先测试**
   ```python
   # 先采集1-2只股票，1-2天的数据
   STOCK_CODES = ['00700']
   START_DATE = '2024-01-31'
   END_DATE = '2024-01-31'
   ```

2. **定期查看日志文件**
   - 了解采集状态
   - 发现潜在问题
   - 优化采集策略

3. **合理安排采集时间**
   - 大批量采集选择夜间或周末
   - 日常同步选择收盘后
   - 避免交易时段采集

4. **备份重要数据**
   - 定期备份数据库
   - 保存日志文件
   - 记录采集配置

---

## ✅ 快速检查命令

```bash
# 检查Python环境
python --version

# 检查依赖包
pip list | grep akshare
pip list | grep pandas
pip list | grep sqlalchemy

# 查看数据库中的港股数量
# （需要数据库客户端）
SELECT COUNT(*) FROM stock_basic_info_hk;

# 查看最近的历史数据
SELECT MAX(date) FROM historical_quotes_hk;
```

---

**祝您使用愉快！如有问题，请查看详细文档或日志文件。** 🎉
