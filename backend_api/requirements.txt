# 运行时依赖；测试与代码风格见 requirements-dev.txt
# 最小化安装见 requirements-minimal.txt

# Web 框架
fastapi>=0.115,<0.116
uvicorn>=0.34.2,<0.35.0
python-multipart>=0.0.20,<0.0.21
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.1.0.post1

# 数据库
sqlalchemy>=2.0.36,<2.1
psycopg2-binary==2.9.10

# 数据处理（本地完整安装亦建议与 minimal 一致，避免 Py3.13 无 wheel）
pandas>=2.2.3,<3
numpy>=2.1.0,<3
# RPE/个股分析 KDE 阻力支撑（backend_core.strategies.rpe.kde_levels）；缺省会退回直方图
scipy>=1.7.0,<2
akshare>=1.17.70
baostock>=0.8.8
openpyxl>=3.1.5,<4.0.0
xlrd>=2.0.1

# 工具
python-dotenv==1.0.1
pydantic>=2.11.7,<3
aiohttp==3.11.18
# env_sync 管理端 pull/push 调用远端 HTTP
httpx==0.26.0

# 报告导出（admin/pvfrs_admin_routes：HTML -> PDF，延迟 import）
xhtml2pdf

# 系统监控（admin/system_monitoring 经 backend_core.monitoring，需 psutil）
psutil>=5.9.0

# 可选（仓库内无直接 import；若你本地使用 Alembic CLI / 异步 SQLite 再装）
# alembic==1.13.1
# aiosqlite==0.20.0
