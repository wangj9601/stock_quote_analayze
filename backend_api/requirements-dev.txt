# backend_api：开发 / 测试 / 代码风格（不包含在 requirements.txt）
# 安装示例：
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

black==24.1.1
isort==5.13.2
flake8==7.0.0

pytest==8.0.0
pytest-asyncio==0.23.5
httpx==0.26.0

# 集成测试与脚本（backend_api/test、quick_test 等使用 requests，运行时路由未直接 import）
requests>=2.32.1,<3.0.0
