# backend_api 最小运行时（无测试依赖；与源码直接 import 对齐）
# 不含 PDF 导出时使用 pip uninstall xhtml2pdf 或按需安装 xhtml2pdf

fastapi>=0.115,<0.116
uvicorn>=0.34.2,<0.35.0
python-multipart>=0.0.20,<0.0.21
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.1.0.post1

# Py3.13：2.0.27 会在 typing 上与 3.13 冲突（SQLCoreOperations/TypingOnly）；需 2.0.31+
sqlalchemy>=2.0.36,<2.1
psycopg2-binary==2.9.10

# Python 3.13 + Windows：numpy 1.26 无 cp313 wheel，会走源码编译并依赖 VS/C 编译器；请使用带 wheel 的版本。
pandas>=2.2.3,<3
numpy>=2.1.0,<3
akshare>=1.17.70
openpyxl>=3.1.5,<4.0.0
xlrd>=2.0.1

python-dotenv==1.0.1
# pydantic-core 无 wheel 时会走 maturin/Rust，需本机 link.exe；2.11.7+ 在 cp313-win_amd64 上有官方 wheel。
pydantic>=2.11.7,<3
aiohttp==3.11.18

psutil>=5.9.0

# PDF 导出功能另装：xhtml2pdf
