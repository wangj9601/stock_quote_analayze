# 港股历史数据手动采集程序使用说明

## 概述

`manual_hk_historical_collection.py` 是一个用于手动采集港股历史数据的Python程序。该程序基于现有的港股历史数据采集器 (`HKHistoricalQuoteCollector`)，提供了灵活的采集模式和完善的日志记录功能。

## 功能特点

1. **多种采集模式**
   - 指定股票采集：采集指定股票代码列表的历史数据
   - 全量采集：采集数据库中所有港股的历史数据
   - 自选股采集：采集自选股列表中的股票历史数据

2. **自动指标计算**
   - MACD（指数平滑异同移动平均线）
   - KDJ（随机指标）
   - RSI（相对强弱指标）
   - MA（移动平均线）
   - BOLL（布林带）
   - MAVOL（成交量移动平均）
   - PVFRS（均值频率共振指标）

3. **完善的日志记录**
   - 控制台实时输出
   - 日志文件持久化保存
   - 详细的进度和错误信息

## 工作原理

该程序的工作流程如下：

1. **数据来源**：从 `stock_realtime_quote_hk` 表读取指定日期的实时行情数据
2. **数据同步**：将实时数据同步到 `historical_quotes_hk` 历史行情表
3. **指标计算**：为自选股中的股票计算各项技术指标
4. **数据存储**：将指标数据保存到对应的指标表中

**重要提示**：本程序依赖于实时行情数据，因此需要先确保 `stock_realtime_quote_hk` 表中有对应日期的数据。

## 使用前准备

### 1. 环境要求

- Python 3.8+
- 已安装项目依赖包（akshare, pandas, sqlalchemy等）
- 数据库已初始化并包含港股基础信息

### 2. 数据库表结构

确保以下表已创建：
- `stock_basic_info_hk`：港股基础信息表
- `stock_realtime_quote_hk`：港股实时行情表
- `historical_quotes_hk`：港股历史行情表
- `watchlist`：自选股表
- 各项指标表（macd_indicators, kdj_indicators等）

### 3. 实时数据准备

在采集历史数据之前，需要先采集对应日期的实时行情数据。可以通过以下方式：
- 使用后台数据采集服务
- 使用管理后台的数据采集功能
- 运行实时行情采集程序

## 使用方法

### 方式一：修改配置参数后运行

1. 打开 `manual_hk_historical_collection.py` 文件

2. 找到配置参数部分（文件末尾）：

```python
# ========== 配置采集参数 ==========
# 日期范围
START_DATE = '2024-01-01'  # 开始日期
END_DATE = '2024-01-31'    # 结束日期

# 采集模式选择
COLLECTION_MODE = 'specified'  # 'specified', 'all', 'watchlist'

# 指定股票代码列表（仅在 collection_mode='specified' 时需要）
STOCK_CODES = [
    '00700',  # 腾讯控股
    '09988',  # 阿里巴巴-SW
    '01810',  # 小米集团-W
]
```

3. 根据需要修改参数

4. 运行程序：
```bash
python manual_hk_historical_collection.py
```

5. 确认采集参数后输入 `y` 开始采集

### 方式二：在代码中调用函数

```python
from manual_hk_historical_collection import collect_data

# 示例1：采集指定股票
collect_data(
    start_date='2024-01-01',
    end_date='2024-01-31',
    stock_codes=['00700', '09988', '01810'],
    collection_mode='specified'
)

# 示例2：采集所有港股
collect_data(
    start_date='2024-01-01',
    end_date='2024-01-31',
    collection_mode='all'
)

# 示例3：采集自选股
collect_data(
    start_date='2024-01-01',
    end_date='2024-01-31',
    collection_mode='watchlist'
)
```

## 采集模式详解

### 1. 指定股票采集 (specified)

**适用场景**：
- 只需要采集少量特定股票的数据
- 测试采集功能
- 补充特定股票的缺失数据

**使用方法**：
```python
COLLECTION_MODE = 'specified'
STOCK_CODES = ['00700', '09988', '01810']
```

**注意事项**：
- 股票代码必须是5位数字格式（如 '00700'）
- 程序会自动验证股票代码是否存在于数据库中
- 无效的股票代码会被自动跳过

### 2. 全量采集 (all)

**适用场景**：
- 初次建立历史数据库
- 批量补充所有港股的历史数据
- 定期全量更新

**使用方法**：
```python
COLLECTION_MODE = 'all'
# 不需要指定 STOCK_CODES
```

**注意事项**：
- 会采集 `stock_basic_info_hk` 表中的所有股票
- 采集时间较长（取决于股票数量和日期范围）
- 建议在网络稳定时进行

### 3. 自选股采集 (watchlist)

**适用场景**：
- 只关注自选股的历史数据
- 定期更新自选股数据
- 节省采集时间和资源

**使用方法**：
```python
COLLECTION_MODE = 'watchlist'
# 不需要指定 STOCK_CODES
```

**注意事项**：
- 会采集 `watchlist` 表中的所有股票
- 技术指标仅对自选股计算
- 如果自选股列表为空，采集会终止

## 日志说明

### 日志输出位置

1. **控制台输出**：实时显示采集进度和状态
2. **日志文件**：`manual_hk_collection.log`（程序运行目录下）

### 日志级别

- **INFO**：正常信息（采集进度、成功信息）
- **WARNING**：警告信息（无数据、股票代码无效等）
- **ERROR**：错误信息（采集失败、异常等）

### 日志示例

```
2024-01-15 10:00:00 - __main__ - INFO - ================================================================================
2024-01-15 10:00:00 - __main__ - INFO - 港股历史数据手动采集程序启动
2024-01-15 10:00:00 - __main__ - INFO - 采集日期范围: 2024-01-01 至 2024-01-31
2024-01-15 10:00:00 - __main__ - INFO - 采集模式: specified
2024-01-15 10:00:00 - __main__ - INFO - ================================================================================
2024-01-15 10:00:01 - __main__ - INFO - 本次将采集 3 只股票的历史数据
2024-01-15 10:00:01 - __main__ - INFO - 共需采集 22 个交易日的数据
2024-01-15 10:00:02 - __main__ - INFO - 
进度: [1/22] 正在采集 20240102 的数据...
2024-01-15 10:00:03 - __main__ - INFO - ✓ 20240102 数据采集成功
```

## 常见问题

### Q1: 采集失败，提示"未找到港股实时行情表中的数据"

**原因**：`stock_realtime_quote_hk` 表中没有对应日期的数据

**解决方法**：
1. 先运行实时行情采集程序
2. 或使用后台数据采集服务采集实时数据
3. 确认采集日期是否为交易日

### Q2: 采集成功但没有计算技术指标

**原因**：技术指标仅对自选股中的股票计算

**解决方法**：
1. 将需要计算指标的股票添加到自选股列表
2. 或修改代码逻辑，为所有股票计算指标

### Q3: 股票代码验证失败

**原因**：股票代码不存在于 `stock_basic_info_hk` 表中

**解决方法**：
1. 检查股票代码格式（应为5位数字）
2. 确认股票代码是否正确
3. 运行港股基础信息采集程序更新股票列表

### Q4: 采集速度慢

**原因**：
- 日期范围过大
- 股票数量过多
- 网络速度慢

**解决方法**：
1. 分批采集，缩小日期范围
2. 使用指定股票模式，减少股票数量
3. 在网络稳定时进行采集

## 性能优化建议

1. **分批采集**：将大范围日期分成多个小批次
2. **错误重试**：对失败的日期进行重试
3. **并发控制**：考虑使用多线程（需注意数据库锁）
4. **增量采集**：只采集缺失的数据

## 数据验证

采集完成后，建议进行以下验证：

1. **数据完整性检查**
```sql
-- 检查历史行情数据
SELECT date, COUNT(*) as stock_count 
FROM historical_quotes_hk 
WHERE date BETWEEN '2024-01-01' AND '2024-01-31'
GROUP BY date 
ORDER BY date;

-- 检查指标数据
SELECT date, COUNT(*) as indicator_count 
FROM macd_indicators 
WHERE date BETWEEN '2024-01-01' AND '2024-01-31' 
  AND market_type = 'HK'
GROUP BY date 
ORDER BY date;
```

2. **数据准确性检查**
- 随机抽取几只股票，对比数据源验证数据准确性
- 检查技术指标计算是否正确

## 注意事项

1. **数据依赖**：本程序依赖实时行情数据，需先采集实时数据
2. **交易日判断**：程序会自动跳过周末，但不会判断节假日
3. **指标计算**：技术指标仅对自选股计算，如需全量计算需修改代码
4. **数据库锁**：大量并发写入可能导致数据库锁，建议单线程运行
5. **日志文件**：长期运行会产生大量日志，建议定期清理

## 扩展功能

如需扩展功能，可以修改以下部分：

1. **添加新的采集模式**：在 `collect_data` 函数中添加新的模式判断
2. **自定义指标计算**：修改 `HKHistoricalQuoteCollector` 类
3. **添加数据验证**：在采集后添加数据验证逻辑
4. **实现断点续传**：保存采集进度，支持中断后继续

## 技术支持

如遇到问题，请：
1. 查看日志文件 `manual_hk_collection.log`
2. 检查数据库表结构和数据
3. 确认网络连接和数据源可用性
4. 查看相关代码文档和注释
