{
  "name": "stock-web-auto",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:smoke": "playwright test --project=chromium --grep @smoke",
    "test:cases": "playwright test --project=chromium --grep @case",
    "test:all": "playwright test --project=chromium",
    "test:headed": "playwright test --project=chromium --headed",
    "report": "playwright show-report",
    "excel:convert": "tsx scripts/excel-to-json.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^25.6.0",
    "dotenv": "^16.6.1",
    "tsx": "^4.20.6",
    "xlsx": "^0.18.5"
  }
}
