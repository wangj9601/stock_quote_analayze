# 实现计划: 一阳穿三线选股策略

## 概述

本实现计划将"一阳穿三线"选股策略分解为可执行的编码任务。策略识别股价在均线系统中出现带量长阳线并突破至少三条移动平均线的技术形态。

## 任务

- [x] 1. 创建策略核心类和基础结构
  - 创建 `backend_api/stock/one_yang_three_lines_strategy.py` 文件
  - 定义 `OneYangThreeLinesStrategy` 类
  - 添加必要的导入和日志配置
  - _需求: 1.1, 1.2, 1.3_

- [x] 2. 实现移动平均线计算功能
  - [x] 2.1 实现 `calculate_moving_averages()` 方法
    - 计算MA5, MA10, MA20, MA30, MA60, MA120
    - 使用收盘价作为计算基础
    - 处理数据不足的情况
    - _需求: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.8_

  - [x]* 2.2 编写属性测试: 均线计算正确性
    - **属性1: 移动平均线计算正确性**
    - **验证: 需求 2.1-2.7**
    - 生成随机价格数据,验证MA计算结果等于最近N日收盘价的算术平均值

- [x] 3. 实现长阳线识别功能
  - [x] 3.1 实现 `check_long_yang_candle()` 方法
    - 判断是否为阳线(收盘价>开盘价)
    - 计算实体长度和K线总长度
    - 判断实体占比是否>=70%
    - 判断涨幅是否>=3%
    - _需求: 4.1, 4.2, 4.3, 4.4, 4.5_

  - [ ]* 3.2 编写属性测试: 长阳线识别
    - **属性3: 长阳线识别**
    - **验证: 需求 4.1-4.5**
    - 生成随机K线数据,验证长阳线识别的正确性

- [x] 4. 实现穿线识别功能
  - [x] 4.1 实现 `check_cross_three_lines()` 方法
    - 检查收盘价是否大于至少3条均线
    - 检查开盘价是否小于这些均线中的至少2条
    - 检查最低价是否低于这些均线中的至少1条
    - 记录具体穿越的均线列表
    - 返回穿越数量
    - _需求: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6_

  - [ ]* 4.2 编写属性测试: 穿越有效性
    - **属性5: 穿越有效性**
    - **验证: 需求 5.1-5.6**
    - 生成随机K线和均线数据,验证穿越判断的正确性

- [x] 5. 实现成交量验证功能
  - [x] 5.1 实现 `check_volume_increase()` 方法
    - 计算前5日平均成交量
    - 判断当日成交量是否>=平均值的2倍
    - 获取换手率数据
    - 判断换手率范围并生成警告
    - _需求: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6_

  - [ ]* 5.2 编写属性测试: 放量突破判断
    - **属性6: 放量突破判断**
    - **验证: 需求 6.1, 6.2**
    - 生成随机成交量数据,验证放量判断的正确性

  - [ ]* 5.3 编写属性测试: 换手率警告一致性
    - **属性7: 换手率警告一致性**
    - **验证: 需求 6.4-6.6**
    - 生成不同换手率的数据,验证警告逻辑的正确性

- [x] 6. 实现位置判别功能
  - [x] 6.1 实现 `check_position_type()` 方法
    - 计算60日最高价
    - 计算当前价格相对最高价的回撤幅度
    - 根据回撤幅度判断位置类型(低位/中位/高位)
    - 对高位突破生成风险提示
    - _需求: 7.1, 7.2, 7.3, 7.4, 7.5_

  - [ ]* 6.2 编写属性测试: 位置类型判断
    - **属性8: 位置类型判断**
    - **验证: 需求 7.2-7.4**
    - 生成不同回撤幅度的数据,验证位置类型判断的正确性

- [x] 7. 实现乖离率计算功能
  - [x] 7.1 实现 `calculate_bias()` 方法
    - 计算BIAS5, BIAS10, BIAS30
    - 使用公式: BIAS = (价格 - MA) / MA × 100%
    - 对BIAS30>10%生成风险提示
    - _需求: 8.1, 8.2, 8.3, 8.4, 8.5_

  - [ ]* 7.2 编写属性测试: 乖离率计算正确性
    - **属性9: 乖离率计算正确性**
    - **验证: 需求 8.1-8.3**
    - 生成随机价格和均线数据,验证乖离率计算公式

  - [ ]* 7.3 编写属性测试: 高乖离率风险提示
    - **属性10: 高乖离率风险提示**
    - **验证: 需求 8.4**
    - 验证BIAS30>10%时是否产生风险提示

- [x] 8. 实现信号质量评分功能
  - [x] 8.1 实现 `calculate_signal_score()` 方法
    - 根据穿越均线数量评分(3条=20分,4条=30分,5条=40分,6条=50分)
    - 根据成交量倍数评分(>=2倍=20分,>=3倍=25分)
    - 根据换手率评分(3%-10%=15分,其他=5分)
    - 根据位置类型评分(低位=15分,中位=10分,高位=0分)
    - 根据乖离率评分(<5%=10分,5%-10%=5分,>10%=0分)
    - _需求: 5.7, 9.3, 3.5_

  - [ ]* 8.2 编写属性测试: 穿线数量单调性
    - **属性4: 穿线数量单调性**
    - **验证: 需求 5.7**
    - 验证穿越均线数量越多,评分越高

- [x] 9. 实现策略主函数
  - [x] 9.1 实现 `screening_one_yang_three_lines_strategy()` 方法
    - 查询A股股票列表(排除ST股票)
    - 获取最近20个交易日的历史数据
    - 对每只股票执行策略检查
    - 计算所有指标和评分
    - 按评分降序排列结果
    - 处理异常并记录日志
    - _需求: 1.1, 1.2, 1.3, 10.1-10.7_

  - [ ]* 9.2 编写属性测试: ST股票排除
    - **属性2: ST股票排除**
    - **验证: 需求 1.2**
    - 验证结果中不包含任何ST股票

  - [ ]* 9.3 编写属性测试: 输出完整性
    - **属性12: 输出完整性**
    - **验证: 需求 10.1-10.6**
    - 验证输出包含所有必需字段

  - [ ]* 9.4 编写属性测试: 结果排序单调性
    - **属性11: 结果排序单调性**
    - **验证: 需求 10.7**
    - 验证结果按评分降序排列

- [x] 10. 检查点 - 确保核心策略逻辑测试通过
  - 运行所有单元测试和属性测试
  - 确认所有测试通过
  - 如有问题,请向用户询问

- [x] 11. 添加API路由
  - [x] 11.1 在 `stock_screening_routes.py` 中添加新路由
    - 定义GET接口 `/api/screening/one-yang-three-lines`
    - 添加参数验证(可选的分页和日期范围参数)
    - 调用策略主函数
    - 格式化返回JSON结果
    - 添加错误处理和日志记录
    - _需求: 12.1, 12.2, 12.3, 12.4, 12.5_

  - [ ]* 11.2 编写单元测试: API响应格式
    - **属性14: API响应格式**
    - **验证: 需求 12.4**
    - 测试API返回的JSON格式是否正确

  - [ ]* 11.3 编写单元测试: API错误处理
    - 测试数据库连接失败的情况
    - 测试参数验证失败的情况
    - _需求: 12.5_

- [x] 12. 前端集成(可选)
  - [x] 12.1 在选股策略页面添加"一阳穿三线"选项卡
    - 修改 `frontend/src/pages/Screening.tsx`
    - 添加新的Tab组件
    - _需求: 13.1_

  - [x] 12.2 实现结果表格展示
    - 创建表格列定义(包含所有关键指标)
    - 实现刷新筛选按钮
    - 添加位置类型的颜色标识
    - 显示风险提示
    - 实现股票代码点击跳转
    - _需求: 13.2, 13.3, 13.4, 13.5, 13.6_

- [x] 13. 数据持久化(可选)
  - [x] 13.1 创建信号存储表
    - 创建 `one_yang_three_lines_signals` 表
    - 添加唯一约束(code, signal_date)
    - _需求: 11.1, 11.2, 11.4_

  - [x] 13.2 实现信号保存功能
    - 在策略主函数中添加保存逻辑
    - 处理保存失败的情况
    - 实现去重逻辑
    - _需求: 11.3, 11.4_

  - [ ]* 13.3 编写属性测试: 数据去重
    - **属性13: 数据去重**
    - **验证: 需求 11.4**
    - 验证同一股票同一日期只保存一条记录

- [x] 14. 集成测试和性能优化
  - [x] 14.1 编写集成测试
    - 测试完整的端到端流程
    - 测试API调用到数据库查询到结果返回
    - 使用真实数据库进行测试

  - [x] 14.2 性能测试和优化
    - 测试处理5000只股票的执行时间
    - 如果超过5分钟,进行优化(批量查询、索引优化等)
    - 记录性能指标

- [x] 15. 最终检查点 - 确保所有测试通过
  - 运行完整的测试套件
  - 确认所有功能正常工作
  - 检查日志输出是否正确
  - 如有问题,请向用户询问

## 注意事项

- 任务标记 `*` 的为可选测试任务,可以跳过以加快MVP开发
- 每个任务都引用了相关的需求编号,便于追溯
- 属性测试使用hypothesis框架,每个测试运行100次以上
- 单元测试使用pytest框架
- 实现时参考现有的 `low_nine_strategy.py` 代码模式
- 复用 `backend_core/utils/ma_calculator.py` 中的MA计算函数(如果适用)
