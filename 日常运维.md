# 股票分析系统生产环境-日常运维说明文档

## 1. nginx 502 Bad Gateway

    - windows 命令行 查看端口对应进程
        netstat -ano | findstr :443 | findstr LISTENING

    - 根据 PID 查找进程名称
        tasklist | findstr 1828

    - 查找特定端口（例如 443）并显示进程名
        Get-NetTCPConnection -LocalPort 443 -State Listen | Select-Object LocalAddress, LocalPort, State, @{Name="Process"; Expression={(Get-Process -Id $_.OwningProcess).ProcessName}}, OwningProcess

    - windows 命令行 kill端口对应进程
        Stop-Process -Name "nginx" -Force

    - windows 命令行 如何优雅启动nginx进程
        start nginx

    - 总结
        1) 强制结束	taskkill /im nginx.exe /f	清除所有旧的、来源不明的 Nginx 进程
        2) 验证	tasklist /fi "imagename eq nginx.exe"	确认进程已全部结束
        3) 重新启动	.\nginx.exe	从你想要的目录启动新的 Nginx 实例
        4) 未来停止	.\nginx.exe -s stop	使用同一路径下的命令来优雅停止
        对于你现在的情况，直接执行 taskkill /im nginx.exe /f 是最快最有效的解决方法。

## 2. admin管理端打包注意事项

    - 生产环境admin打包执行步骤
        1) .\admin\deploy-env.bat production
        2) npm run build
        3) 上传dist目录到远程主机
        4） python -m http.server 8001

## 3. backend_api, backend_core, frontend打包注意事项

    python package.py

## 4.powershell 重启机器

    - 延迟重启（30秒后）
    shutdown /r /t 30

## 5.历史数据采集--指定日期

python -m backend_core.data_collectors.tushare.main --type historical --date yyyymmmdd

## 6.生成数据库表脚本

pg_dump -h 192.168.16.4 -p 8432 -U postgres -t sim_trade_accounts --schema-only stock_analysis

pg_dump -h 192.168.16.4 -p 8432 -U postgres -d stock_analysis -t sim_trade_accounts --schema-only > sim_trade_accounts_create_script.sql

pg_dump -h 192.168.16.4 -p 8432 -U postgres -d stock_analysis -t sim_trade_accounts --schema-only > "C:\work\sim_trade_accounts_create_script.sql"





git reset --hard origin/main



cd E:\wangxw\股票分析软件\编码\stock_quote_analayze

## 7. 发布包（开发机仅打包 + 服务器手工上传）

当前默认流程：**本机只生成发布 zip**，不连接 SSH。脚本完成后根据终端提示，把 `dist\stock_quote_release_*.zip` 拷到服务器（如 RDP、共享盘），再在服务器上执行 `release.ps1`。

开发机构建发布包：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy\deploy.ps1
```

可选：跳过 `package.py`（仅 admin 构建 + 打 zip）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy\deploy.ps1 -SkipPackagePy
```

服务器：把 zip 放到例如 `C:\work\stock_quote_analayze\tmp\`，再执行 **`release.ps1`**。**若服务端从未初始化过，请先完成 §8**（首次要用 **`src\scripts\deploy\release.ps1`**，不能直接依赖尚未生成的 **`current`**）。

**低磁盘 / 不用 NSSM、用手工 Python 进程发版**（**`-ManualProcessDeploy`** 及完整命令）：见 **§10.9～§10.13**；结束手工起的 Python 进程树见 **§10.14**。

已有 **`current`** 后示例：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\current\scripts\deploy\release.ps1" `
  -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_YYYYMMDD_HHmmss.zip" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0"
```

若仍需 **SSH 一键上传**（开发机已能访问服务器 22 端口），可加 `-RemoteDeploy` 并带上 `-ServerHost`、`-ServerUser` 等参数（见 `docs/prod/windows-cvm-one-click-deploy.md`）。

---

## 8. 服务端首次初始化（尚未做过时请按顺序执行）

下面以 **`DeployRoot = C:\work\stock_quote_analayze`**、源码检出目录 **`...\src`** 为例（与 `current` / `shared` / `releases` 约定一致）。路径不同请自行替换。

### 8.1 环境与工具（一次性）

在服务器（Administrator）上确认已安装或准备好：

| 项 | 说明 |
|----|------|
| Python 3.x | 命令行可执行 `python`，且已加入 PATH |
| Node.js + npm | 用于 `release.ps1` 内构建 admin |
| NSSM | **`install_services.ps1` 注册 Windows 服务时需要**；下载见 https://nssm.cc/ ，将 **`nssm.exe`** 放到 **`C:\work\stock_quote_analayze\tools\nssm.exe`**（与脚本默认一致，与 `tools\nginx-1.28.0` 同级），或在执行 **`install_services.ps1`** 时用 **`-NssmExe`** 指定路径。**`bootstrap_windows.ps1` 不要求 NSSM 已存在**，未安装时只会警告并继续创建目录与 `.env`。 |
| PostgreSQL | 库已建、`DATABASE_URL` 可用（按项目实际） |
| Nginx | 你已使用的目录，例如 **`C:\work\stock_quote_analayze\tools\nginx-1.28.0`** |

### 8.2 仓库与脚本（至少要有部署脚本）

在服务器上克隆或拷贝仓库，保证存在：

`C:\work\stock_quote_analayze\src\scripts\deploy\bootstrap_windows.ps1`  
`C:\work\stock_quote_analayze\src\scripts\deploy\install_services.ps1`  
`C:\work\stock_quote_analayze\src\scripts\deploy\release.ps1`

### 8.3 初始化目录与 `.env`（只做一次）

管理员 PowerShell：

```powershell
cd C:\work\stock_quote_analayze\src
powershell -ExecutionPolicy Bypass -File .\scripts\deploy\bootstrap_windows.ps1 -DeployRoot "C:\work\stock_quote_analayze"
```

然后编辑 **`C:\work\stock_quote_analayze\shared\.env`**，至少配置：

- `ENVIRONMENT=production`
- `BACKEND_PORT=5000`（若与 `nginx`/前端约定一致）
- `DATABASE_URL=...`（真实连接串）

保存。

### 8.3.1 部署前：手工启动的进程要不要先停？

**结论（简要）：**

- **当前仍是「手动起的 Python / 前台脚本」**（例如直接运行 `start_backend_api.py`、`npm run dev`、占 **5000 / 8000 / 8001** 等端口）：在**第一次**执行 **`release.ps1`**，以及即将执行 **`install_services.ps1` 把 NSSM 服务拉起来之前**，**建议先停掉这些手工进程**（可用 **`scripts\deploy\Kill-StockQuotePython.ps1`**，说明见 **§10.14**）。否则容易 **端口被占用**，新服务起不来；少数情况下还会因进程锁住工作目录下的文件导致解压/覆盖不顺利。
- **`release.ps1` 里的 `Restart-Service stock-quote-*`** 只作用于 **Windows 服务**；若你还没装 NSSM、没有这些服务，脚本里会打印 `[SKIP] 服务不存在`，**不会自动结束**你在任务管理器里手工开的 `python.exe` / `node.exe`。
- **已经用 NSSM 注册并运行** `stock-quote-api` / `stock-quote-core` / `stock-quote-notify` 之后：日常发版一般 **不必** 先手工停进程——`release.ps1` 会尝试 **重启这三项服务** 并切换 `current`。若某次升级失败、怀疑文件被占用，可 **`Stop-Service stock-quote-*`** 后再跑一次 `release.ps1` 排查。
- **Nginx**：只要路径与配置未改，通常 **不必** 为了发版而停止；`release.ps1` 只做 **`nginx -t` 与 reload**。若你手工起的 Nginx 与生产路径不一致，再按 §1 自行统一。

### 8.4 开发机打 zip 并拷到服务器

在开发机执行（见 §7），得到 **`dist\stock_quote_release_*.zip`**。  
用远程桌面、共享文件夹等方式，把 zip 拷到服务器，例如：

**`C:\work\stock_quote_analayze\tmp\stock_quote_release_YYYYMMDD_HHmmss.zip`**

（可先建目录：`New-Item -ItemType Directory -Force -Path C:\work\stock_quote_analayze\tmp`）

### 8.5 第一次执行 `release.ps1`（生成 `current`，尚无 `current` 时）

**含义**：此时 **`DeployRoot` 下还没有 `current`**（例如尚无 **`C:\work\stock_quote_analayze\current`**）。必须用 **Git 仓库里的脚本**，即 **`...\src\scripts\deploy\release.ps1`**（不要用尚未存在的 **`...\current\scripts\...`**）。

成功后会多出：**`...\releases\<时间戳>`**（备份）、**`...\current`**（线上运行目录）、并完成解压、`pip`、`npm`、可选迁移、`nginx -t` / reload；失败会尝试回滚。

---

#### 事前检查（按顺序）

1. **已完成 §8.3**：已执行 `bootstrap_windows.ps1`，存在 **`...\shared\.env`**，且已按库连接等填好真实内容。
2. **开发机已打包**：本地 **`dist\stock_quote_release_*.zip`** 已复制到服务器（建议路径如下）。
3. **手工进程（可选）**：若仍有手动起的后端/前端占端口，见 §8.3.1，建议先停。
4. **目录**：临时放 zip 的目录需存在，例如：

```powershell
New-Item -ItemType Directory -Force -Path "C:\work\stock_quote_analayze\tmp"
```

假设磁盘上的 zip **绝对路径**为（文件名随你实际上传修改）：

`C:\work\stock_quote_analayze\tmp\stock_quote_release_20260509_153000.zip`

---

#### 参数说明（对应 `release.ps1`）

| 参数 | 是否必填 | 含义 |
|------|----------|------|
| **`-PackagePath`** | **必填** | 服务器上 **zip 的完整路径**（建议放在 `DeployRoot\tmp\`，文件名与上传的一致）。 |
| **`-DeployRoot`** | 可选（有默认值） | 部署根目录，包含 **`releases`**、**`shared`**、解压后将出现 **`current`**。此处为 **`C:\work\stock_quote_analayze`**。 |
| **`-NginxHome`** | 可选 | Nginx 安装目录（其下须有 **`nginx.exe`**）。未指定则脚本用 PATH 里的 `nginx`。示例：**`...\tools\nginx-1.28.0`**。 |
| **`-PythonExe`** | 可选 | 默认 `python`，若有多版本可改为 `py -3.11` 等可执行名。 |
| **`-NpmExe`** | 可选 | 默认 `npm`。 |

---

#### 管理员 PowerShell：首次发布命令（可复制）

在服务器上以 **Administrator** 打开 **PowerShell**，把 **`PackagePath`** 里的文件名改成你 **真实 zip 文件名**（可从资源管理器地址栏复制完整路径）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\src\scripts\deploy\release.ps1" `
  -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_20260509_142814.zip" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0"
```

单行写法（无换行反引号）等价：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\src\scripts\deploy\release.ps1" -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_20260509_153000.zip" -DeployRoot "C:\work\stock_quote_analayze" -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0"
```

---

#### 执行过程中脚本大致会做

1. 检查 **`PackagePath`** 指向的 zip 是否存在。  
2. 解压到 **`...\releases\<yyyyMMdd_HHmmss>`**。  
3. 若存在 **`...\shared\.env`**，复制到本次解压目录（供运行时使用）。  
4. **`pip`** 安装 **`requirements-prod.txt`**（内含 **`backend_api/requirements-minimal.txt`** 与 **`backend_core/requirements-minimal.txt`**）；进入 **`admin`** 执行 **`npm install`**、**`npm run build`**。  
5. 若存在 **`migrate_db.py`**，执行迁移。  
6. 将 **`current`** 备份并切换到新版本目录。  
7. 尝试 **`Restart-Service stock-quote-*`**（尚未装 NSSM 时会 **`[SKIP] 服务不存在`**，属正常）。  
8. **`nginx.exe -t`**，通过后 **`nginx -s reload`**（`-NginxHome` 指定时使用该目录下的 **`nginx.exe`**）。  
9. 脚本内健康检查会访问本机与域名（若外网/HTTPS 未就绪可能失败）；失败会尝试 **回滚 `current`**。

#### 为何 release 会联网下载 / 执行 pip（即使以前生产已经能跑）

- **脚本设计**：每次发版都会解压出一个**新的** `releases\<时间戳>` 再迁到 **`current`**。zip 里通常**不带**完整虚拟环境（`venv`），运行仍依赖本机 Python。脚本里固定执行 **`pip install -r requirements-prod.txt`**（该文件 **`-r`** 引用 **`backend_api/requirements-minimal.txt`**（API 最小运行时；PDF 另装 **`xhtml2pdf`**）与 **`backend_core/requirements-minimal.txt`**），是为了：**新版本若改了依赖**，服务器上与**生产清单**对齐，避免「代码新了、包还是旧的」。
- **与手工进程的关系**：你以前用手工起的 Python，用的也是**同一套全局或用户级 site-packages**。`pip install` 多数包会显示 **already satisfied**，但仍可能访问 PyPI（例如脚本里的 **`pip install --upgrade pip`**、解析版本元数据、补装缺失包），所以会看到下载。
- **网络超时**：访问 `files.pythonhosted.org` 慢，多为 **出口网络/防火墙/国际链路** 问题；可配置 **国内 PyPI 镜像**（如清华、阿里云）或先在服务器上好依赖再发版。
- **若想尽量减少下载**：需要改 **`release.ps1`**（例如增加跳过 pip 的参数，或改为只用固定 **venv**），属于定制行为；默认脚本偏「每次发布自洽、依赖可复现」。

---

#### 成功后请自检

- 存在目录：**`C:\work\stock_quote_analayze\current`**。  
- **`...\shared\logs`** 下可有服务端日志（装好 NSSM 并注册服务后更明显）。

接下来再继续 **§8.6 `install_services.ps1`**（仅需做一次）。

### 8.6 注册 Windows 服务（NSSM，只做一次）

仅在 **`current` 已存在** 之后执行：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\current\scripts\deploy\install_services.ps1" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -StartAfterInstall
```

将注册并可选启动：`stock-quote-api`、`stock-quote-core`、`stock-quote-notify`。日志在 **`...\shared\logs\`**。

### 8.7 以后每次发版（初始化已完成）

1. 开发机重新打包，上传新 zip 到 **`tmp`**。  
2. 服务器执行 **`release.ps1`**（可用 **`current\scripts\deploy\release.ps1`**，与线上一致）：  

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\current\scripts\deploy\release.ps1" `
  -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_新版本.zip" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0"
```

`release.ps1` 会解压、`pip`/`npm`、切换 `current`、重启上述服务并重载 Nginx（脚本内逻辑）。

### 8.8 小结顺序（首次）

`bootstrap` → 编辑 **`shared\.env`** → 上传 zip → **`release.ps1`（首次用 `src` 下脚本）** → **`install_services`** → 浏览器/Nginx 验收。  

更细的拓扑与变量说明见 **`docs/prod/windows-cvm-one-click-deploy.md`**。

---

## 9. 配置 PyPI 镜像（清华 / 阿里云等）

在 **服务器**上执行 **`release.ps1`、手工 `pip install`** 时，若访问 `pypi.org` / `files.pythonhosted.org` 慢或超时，建议为 **当前 Windows 用户**（一般用 **Administrator**）配置国内镜像；配置一次后，该用户下所有 **`pip`** 命令都会默认走镜像。

### 9.1 推荐镜像地址（任选其一）

| 来源 | `index-url`（simple） |
|------|------------------------|
| 清华大学 | `https://pypi.tuna.tsinghua.edu.cn/simple` |
| 阿里云 | `https://mirrors.aliyun.com/pypi/simple/` |
| 腾讯云 | `https://mirrors.cloud.tencent.com/pypi/simple` |
| 豆瓣（备用） | `https://pypi.douban.com/simple/` |

### 9.2 命令行配置（管理员 PowerShell，推荐）

**清华示例：**

```powershell
python -m pip install --upgrade pip
python -m pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

**阿里云示例：**

```powershell
python -m pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
```

查看是否生效：

```powershell
python -m pip config list
```

配置文件位置（当前用户）：一般可在 **`%APPDATA%\pip\pip.ini`**（可用资源管理器地址栏输入 `%APPDATA%\pip` 打开）。

### 9.3 可选：信任镜像主机（少数环境 HTTPS 校验报错时）

```powershell
python -m pip config set global.trusted-host "pypi.tuna.tsinghua.edu.cn mirrors.aliyun.com files.pythonhosted.org pypi.org"
```

（按你实际选用的镜像域名删减；不必一上来就加，报错再加。）

### 9.4 与 `release.ps1` 的关系

`release.ps1` 内调用的是 **`python -m pip`**，会 **自动读取上述全局 pip 配置**，无需改脚本。配置完成后重新执行 **`release.ps1`** 即可。

### 9.5 npm（可选）

若 **`npm install`** 也慢，可单独配置 npm registry（与 pip 无关），例如：

```powershell
npm config set registry https://registry.npmmirror.com
```

---

## 10. 远程部署经验备忘（2026-05 手工进程 / 低磁盘）

以下为近期线上排障与脚本改造汇总，便于下次发版对照。

### 10.1 磁盘与 robocopy

- **`ENOSPC` / npm `TAR_ENTRY_ERROR`**：系统盘或部署盘满。先删旧 **`releases\`** 目录（仅保留最近 1～2 个）、清 **`tmp\*.zip`**、`npm cache clean --force`，再发布。
- **`robocopy failed (exit 9)`**：多为磁盘满或复制失败；**exit 码为位标志**，脚本中会附带说明与目标盘剩余空间提示。
- **`release.ps1`** 可在 **`Build admin`** 前做磁盘检查并可选清理旧 **`releases`**（见脚本参数 **`MinFreeSpaceGBForAdminBuild`**、**`KeepLatestReleases`**）。

### 10.2 两种发布形态（择一）

| 形态 | 说明 |
|------|------|
| **NSSM 服务** | **`install_services.ps1`** 注册服务；**`PYTHONNOUSERSITE=1`** 时依赖必须装进 **`Lib\site-packages`**（**`PIP_USER=0`** + **`pip install -r requirements-prod.txt`**）。 |
| **手工进程（省磁盘、无 NSSM）** | **`release.ps1 -ManualProcessDeploy`**：服务器上 **不跑 npm**（zip 内在开发机构建好 **`admin\dist`**）；可选 **`-SkipPip`**。脚本在 **`DeployRoot\current`** 后台拉起 **5 个进程**（顺序）：**`admin\dist` 上 `python -m http.server`（默认 8001）** → **`start_backend_core`** → **`start_backend_api`** → **`start_scheduler`** → **`start_frontend`**，日志在 **`shared\logs\manual-*.log`**。 |

开发机打包含 **`npm run build`**：执行 **`scripts\deploy\deploy.ps1`**（可选 **`-SkipAdminBuild`**）。

### 10.3 nginx 与 502

- **nginx 反代的是本机端口**，与代码是否在 **`current`** 无关；但必须与进程监听端口一致：**API 5000、主站前端 8000、管理端静态 8001**（默认，可改参数但要同步 **`nginx.conf`** 里 **upstream**）。
- **首页 `/` 502**：优先查 **`8000`** 是否在监听（**`start_frontend.py`**）；**`/admin/login` 能打开但登录报 502**：静态 OK，**`/api/` 仍走 5000**，说明 **API 未起来**（查 **`manual-api.err.log`**）。
- **自检命令**：`netstat -ano | findstr ":5000 "` / **`:8000`** / **`:8001`**；本机 **`curl http://127.0.0.1:8000/`** 等。
- **`release.ps1 -ManualProcessDeploy -NginxHome ...`** 可在 **`nginx -t`** 前校验 **upstream 端口**与 **SSL 证书路径是否存在**（见 **`SkipNginxUpstreamCheck`**）。

### 10.4 Python / 依赖常见问题

- **`ModuleNotFoundError: No module named 'yaml'`**：**`uvicorn --log-config *.yaml`** 需要 **`PyYAML`**。已在 **`backend_api/requirements-minimal.txt`** 增加；服务器执行 **`pip install PyYAML`** 或重装 **`requirements-prod.txt`**。**`start_backend_api.py`** 在无 **`yaml`** 时会跳过 **`--log-config`** 以免直接退出。
- **服务账户与用户目录 pip**：NSSM 使用 **`PYTHONNOUSERSITE=1`** 时，`pip install --user` 装到的包服务进程**读不到**；务必 **`PIP_USER=0`** 装进 **`Lib\site-packages`**（**`install_services.ps1 -PreparePython`** 或文档中的一行 pip）。

### 10.5 Windows 编码（日志重定向）

- **`start_frontend.py`** 在 **GBK 控制台**或 **`Start-Process` 重定向 stdout 到文件**时，**Unicode 符号（如 ✓）** 可能 **`UnicodeEncodeError`**。仓库已改为 **`_print_safe`** + **ASCII 提示**，发版务必确认 **`current\start_frontend.py`** 已更新；若仍异常可删 **`current\__pycache__`** 后再启动。

### 10.6 重新部署时是否要手动 kill

- **`release.ps1 -ManualProcessDeploy`**：在**覆盖 `current` 之前**会结束与 **`-PythonExe` 同目录安装**下的 **`python.exe`/`pythonw.exe`**，镜像完成后再启动新进程；一般**无需手工 kill**。若混用多个 Python 安装路径，需自行对齐 **`PythonExe`**。

### 10.7 脚本路径防呆

- 建议从 **`DeployRoot\current\scripts\deploy\release.ps1`** 执行发布；若只能从仓库 **`scripts\deploy\release.ps1`** 调用，需 **`-AllowScriptPathMismatch`**（见脚本内 **`Assert-ReleaseScriptPathMatchesDeployRoot`**）。

### 10.8 模板文件位置

- **nginx 生产模板**：仓库根目录 **`nginx.conf`** 与 **`docs/prod/nginx.conf`** 同步维护；上线复制到 **`<NginxHome>\conf\nginx.conf`**。

---

### 10.9 开发机：打 zip（admin 已 `npm install` + `npm run build`，产物进包）

在**开发机**仓库根目录执行（生成 **`dist\stock_quote_release_时间戳.zip`**）：

```powershell
cd E:\wangxw\work\stock_quote_analayze
powershell -ExecutionPolicy Bypass -File .\scripts\deploy\deploy.ps1
```

仅复用已有 **`admin\dist`**、跳过本地 npm（一般不推荐）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy\deploy.ps1 -SkipAdminBuild
```

将 **`dist\stock_quote_release_*.zip`** 拷到服务器，例如：

**`C:\work\stock_quote_analayze\tmp\`**

（可先建目录：`New-Item -ItemType Directory -Force -Path "C:\work\stock_quote_analayze\tmp"`）

---

### 10.10 服务器：手工进程模式发版（`release.ps1 -ManualProcessDeploy`）

**含义**：服务器上**不再执行 npm**（zip 内已含 **`current\admin\dist`**）；可选 **跳过 pip**（磁盘紧且依赖已预装）；脚本会结束同 **`PythonExe`** 安装目录下的旧 **`python.exe`**，镜像 **`current`**，再后台启动 **5 个进程**（管理端静态 **`http.server`** → core → api → scheduler → frontend），日志在 **`DeployRoot\shared\logs\manual-*.log`**。

在服务器 **管理员 PowerShell** 执行（请替换 **`PythonExe`**、zip 文件名、路径与 **`NginxHome`**）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\current\scripts\deploy\release.ps1" `
  -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_YYYYMMDD_HHmmss.zip" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -PythonExe "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" `
  -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0" `
  -ManualProcessDeploy `
  -SkipPip `
  -SkipHealthCheck
```

若只能从**仓库侧** **`DeployRoot\scripts\deploy\release.ps1`**（非 **`current\scripts\deploy\`** 下副本）执行，必须加 **`-AllowScriptPathMismatch`**（否则脚本会因路径与 **`DeployRoot\current`** 不一致而中止）。完整示例（**`PackagePath` 按实际上传的 zip 替换**）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\scripts\deploy\release.ps1" `
  -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_20260510_180459.zip" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -PythonExe "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" `
  -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0" `
  -ManualProcessDeploy `
  -SkipPip `
  -SkipHealthCheck `
  -AllowScriptPathMismatch
```

仍需在服务器执行 **pip** 装依赖时：**去掉 `-SkipPip`**（并保持 **`PIP_USER=0`**，避免装进用户目录导致 **`PYTHONNOUSERSITE=1`** 下读不到）：

```powershell
$env:PIP_USER = '0'
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\current\scripts\deploy\release.ps1" `
  -PackagePath "C:\work\stock_quote_analayze\tmp\stock_quote_release_YYYYMMDD_HHmmss.zip" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -PythonExe "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" `
  -NginxHome "C:\work\stock_quote_analayze\tools\nginx-1.28.0" `
  -ManualProcessDeploy `
  -SkipHealthCheck
```

管理端静态端口非默认 **8001** 时，增加例如：

```powershell
  -ManualAdminHttpPort 8001
```

（并与 **`nginx.conf`** 中 **`admin_server`** upstream 一致。）

---

### 10.11 服务器：仅补依赖（与 NSSM / 手工进程通用）

**PyYAML**（`uvicorn --log-config *.yaml` 需要），与 **`PythonExe` 同一解释器**：

```powershell
$env:PIP_USER = '0'
& "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" -m pip install "PyYAML>=6.0.1,<7"
```

或按 **`current\requirements-prod.txt`** 安装全套最小生产依赖：

```powershell
$env:PIP_USER = '0'
cd C:\work\stock_quote_analayze\current
& "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" -m pip install -r .\requirements-prod.txt
```

---

### 10.12 验收与排障命令（502 / 端口）

监听是否与 nginx **upstream（5000 / 8000 / 8001）**一致：

```powershell
netstat -ano | findstr ":5000 "
netstat -ano | findstr ":8000 "
netstat -ano | findstr ":8001 "
```

本机探测（可选）：

```powershell
curl.exe -s -o NUL -w "%{http_code}" http://127.0.0.1:8000/
curl.exe -s -o NUL -w "%{http_code}" http://127.0.0.1:5000/
```

日志路径：

```text
C:\work\stock_quote_analayze\shared\logs\manual-api.err.log
C:\work\stock_quote_analayze\shared\logs\manual-frontend.err.log
C:\work\stock_quote_analayze\shared\logs\manual-admin-http.err.log
```

更新 **`nginx.conf`** 后（模板见 **§10.8**）：

```powershell
cd C:\work\stock_quote_analayze\tools\nginx-1.28.0
.\nginx.exe -t
.\nginx.exe -s reload
```

---

### 10.13 NSSM 服务模式（对照）：注册服务并准备 Python（可选）

已与 **`install_services.ps1`** 约定一致时（**`PYTHONNOUSERSITE=1`**），依赖必须装进 **`Lib\site-packages`**，可用：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\work\stock_quote_analayze\current\scripts\deploy\install_services.ps1" `
  -DeployRoot "C:\work\stock_quote_analayze" `
  -PythonExe "C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe" `
  -NssmExe "C:\work\stock_quote_analayze\tools\nssm.exe" `
  -StartAfterInstall
```

脚本默认会探测依赖并按需 **`pip install -r current\requirements-prod.txt`**（**`PIP_USER=0`**）。仅补依赖也可单独传 **`-PreparePython`**（详见 **`install_services.ps1`** 注释）。

---

### 10.14 Kill 脚本与 uvicorn 子进程（`Kill-StockQuotePython.ps1`）

**脚本路径**：仓库内 **`scripts\deploy\Kill-StockQuotePython.ps1`**；服务器上一般在 **`DeployRoot\current\scripts\deploy\Kill-StockQuotePython.ps1`**（与 `release.ps1` 同目录约定）。

**用途**：在 **Windows** 上结束与本套 **部署目录** 相关的 **`python.exe` / `pythonw.exe`**，便于发版前清端口、或从「手工进程发版」（**§10.9～§10.12**）切回 NSSM 前收尾。

**匹配规则（为何 `-WhatIf` 常只看到约 4 个「根」进程）**

- 仅当进程的 **命令行** 中含有 **`DeployRoot\current\`** 下的下列路径之一（脚本会同时尝试反斜杠/正斜杠形式）才视为 **部署标记命中**：
  - `start_backend_core.py`
  - `start_backend_api.py`
  - `start_scheduler.py`
  - `start_frontend.py`
  - `admin\dist`（手工起 admin 静态站时）
- 生产环境下 **`start_backend_api.py`** 会通过 **`python -m uvicorn backend_api.main:app`** 启动 API；若 **`ENVIRONMENT`/`APP_ENV` 等为 production**，还会加 **`--workers N`**（多进程）。**子进程（worker）的命令行里通常不再出现 `start_backend_api.py` 字符串**，因此 **不会被单独列在 `-WhatIf` 输出里**，但任务管理器里仍会显示为多个 **Python**——这是正常现象。

**实际结束方式（进程树）**

- 脚本对**每一个「根」PID**（匹配到的进程中，**其父进程 PID 不在「同样匹配」的集合里**）执行一次 **`taskkill /F /T /PID <pid>`**，结束 **该进程及其子进程整棵树**（含 uvicorn worker、`--reload` 子进程等）。这样可避免对子进程再执行 `/T` 时出现「无法终止 PID xxx（属于 PID yyy 子进程）」类错误；使用 **`-PythonExe`** 时一次会匹配很多 Python，此逻辑尤其重要。  
- 若仅用 **`Stop-Process`** 或 **`taskkill /F` 不带 `/T`**，容易出现 **根进程没了、子进程仍占用端口** 的情况；当前脚本已优先使用 **`/T`**。

**推荐用法**

```powershell
cd C:\work\stock_quote_analyze\scripts\deploy   # 按实际 DeployRoot 调整

# 先 dry-run：列出「根」PID（父进程不在同批匹配集合内），不真杀
.\Kill-StockQuotePython.ps1 -DeployRoot "C:\work\stock_quote_analyze" -WhatIf

# 确认无误再去掉 -WhatIf
.\Kill-StockQuotePython.ps1 -DeployRoot "C:\work\stock_quote_analyze"
```

**参数摘要**

| 参数 | 说明 |
|------|------|
| **`-DeployRoot`** | 部署根目录（其下应有 **`current`**）。默认示例为 `C:\deploy\stock_quote`；**必须与进程实际启动路径一致**，否则匹配不到。 |
| **`-PythonExe`** | 可选；指定 **`python.exe` 全路径** 时，会额外匹配「同一解释器安装目录下」的进程。**误杀面大**（可能包含本机其它用同一 Python 的程序），仅在有把握时使用；详见脚本内注释。 |
| **`-WhatIf`** | 只打印将处理的根进程信息，**不**执行 `taskkill`。 |

**发版后仍剩若干 Python**

- 若它们 **不是** 上述四个入口脚本的 **子树**（例如本机其它项目、IDE、Jupyter、一次性脚本），本脚本 **不会** 按「部署标记」匹配；请在任务管理器或 **`Get-CimInstance Win32_Process`** 中核对 **命令行** 再决定是否手工结束。
