# 港股历史数据采集程序对比说明

## 概述

我已经为您创建了两个港股历史数据手动采集程序，它们有不同的工作方式和适用场景。

## 程序对比

### 1. manual_hk_historical_collection.py（基于实时数据）

**工作原理：**
- 从 `stock_realtime_quote_hk` 表读取已采集的实时行情数据
- 将实时数据同步到 `historical_quotes_hk` 历史行情表
- 为自选股中的股票计算技术指标

**优点：**
- 速度快，无需调用外部API
- 不受API限流限制
- 数据已经过验证和清洗

**缺点：**
- 依赖实时行情数据，需要先采集实时数据
- 只能采集已有实时数据的日期

**适用场景：**
- 已经运行了实时数据采集服务
- 需要将实时数据归档到历史表
- 定期同步当日数据到历史库

**使用示例：**
```python
# 采集指定股票（需要先有实时数据）
collect_data(
    start_date='2024-01-01',
    end_date='2024-01-31',
    stock_codes=['00700', '09988'],
    collection_mode='specified'
)
```

---

### 2. direct_hk_historical_collection.py（直接从API）

**工作原理：**
- 直接调用 AKShare API 获取历史数据
- 将数据保存到 `historical_quotes_hk` 表
- 自动计算所有技术指标（可配置）

**优点：**
- 不依赖实时数据，可以独立运行
- 可以采集任意历史日期的数据
- 一次性完成数据采集和指标计算

**缺点：**
- 受API限流限制（需要5秒间隔）
- 采集速度较慢
- 依赖网络连接和API稳定性

**适用场景：**
- 初次建立历史数据库
- 补充缺失的历史数据
- 没有实时数据采集服务
- 需要采集很久以前的历史数据

**使用示例：**
```python
# 直接从API采集历史数据
collect_data_direct(
    start_date='2024-01-01',
    end_date='2024-01-31',
    stock_codes=['00700', '09988'],
    collection_mode='specified',
    calculate_indicators_flag=True
)
```

---

## 功能对比表

| 功能特性 | manual_hk_historical_collection.py | direct_hk_historical_collection.py |
|---------|-----------------------------------|-----------------------------------|
| 数据来源 | stock_realtime_quote_hk 表 | AKShare API |
| 是否依赖实时数据 | 是 | 否 |
| 采集速度 | 快 | 慢（5秒/股） |
| API限流 | 无 | 有 |
| 历史数据采集 | 仅已有实时数据的日期 | 任意历史日期 |
| 指标计算 | 仅自选股 | 所有股票（可配置） |
| 网络依赖 | 低 | 高 |
| 适合场景 | 日常同步 | 初始化/补数据 |

---

## 采集模式说明

两个程序都支持以下采集模式：

### 1. 指定股票采集 (specified)
```python
COLLECTION_MODE = 'specified'
STOCK_CODES = ['00700', '09988', '01810']
```
- 只采集指定的股票代码
- 适合测试或采集少量股票

### 2. 全量采集 (all)
```python
COLLECTION_MODE = 'all'
```
- 采集数据库中所有港股
- 适合批量建立历史数据

### 3. 自选股采集 (watchlist)
```python
COLLECTION_MODE = 'watchlist'
```
- 仅 manual_hk_historical_collection.py 支持
- 采集自选股列表中的股票

---

## 使用建议

### 场景1：初次建立历史数据库
**推荐：** `direct_hk_historical_collection.py`

```python
# 采集最近3个月的数据
collect_data_direct(
    start_date='2023-10-01',
    end_date='2024-01-31',
    collection_mode='all',
    calculate_indicators_flag=True
)
```

### 场景2：定期同步当日数据
**推荐：** `manual_hk_historical_collection.py`

```python
# 每天收盘后同步当日数据
from datetime import datetime
today = datetime.now().strftime('%Y-%m-%d')

collect_data(
    start_date=today,
    end_date=today,
    collection_mode='watchlist'
)
```

### 场景3：补充缺失数据
**推荐：** `direct_hk_historical_collection.py`

```python
# 补充特定日期的数据
collect_data_direct(
    start_date='2024-01-15',
    end_date='2024-01-20',
    stock_codes=['00700'],
    collection_mode='specified',
    calculate_indicators_flag=True
)
```

### 场景4：批量更新自选股
**推荐：** `manual_hk_historical_collection.py`

```python
# 快速更新自选股的历史数据
collect_data(
    start_date='2024-01-01',
    end_date='2024-01-31',
    collection_mode='watchlist'
)
```

---

## 性能对比

### 采集100只股票，30天数据

| 程序 | 预计时间 | 网络流量 | CPU使用 |
|-----|---------|---------|---------|
| manual_hk_historical_collection.py | ~1分钟 | 低 | 低 |
| direct_hk_historical_collection.py | ~8.5小时 | 高 | 中 |

**计算说明：**
- direct程序：100股 × 5秒间隔 = 500秒 ≈ 8.3分钟（单次请求）
- 如果每只股票单独请求30天数据，时间会更长

---

## 技术指标计算

### manual_hk_historical_collection.py
- 仅对自选股计算指标
- 在数据同步后自动触发
- 计算以下指标：
  - MACD（需要26天数据）
  - KDJ（需要9天数据）
  - RSI（需要7天数据）
  - MA（需要5-200天数据）
  - BOLL（需要20天数据）
  - MAVOL（需要5-200天数据）
  - PVFRS（需要20天数据）

### direct_hk_historical_collection.py
- 可为所有股票计算指标（通过参数控制）
- 在数据保存后立即计算
- 计算相同的技术指标
- 可通过 `calculate_indicators_flag` 参数关闭

---

## 日志文件

### manual_hk_historical_collection.py
- 日志文件：`manual_hk_collection.log`
- 记录内容：
  - 数据同步进度
  - 指标计算状态
  - 错误和警告信息

### direct_hk_historical_collection.py
- 日志文件：`direct_hk_collection.log`
- 记录内容：
  - API调用状态
  - 数据保存进度
  - 指标计算状态
  - 错误和警告信息

---

## 错误处理

两个程序都包含完善的错误处理：

1. **数据库连接错误**：自动重试和回滚
2. **API调用失败**：记录错误并继续下一个
3. **数据验证失败**：跳过无效数据
4. **指标计算失败**：记录警告但不中断采集

---

## 最佳实践

### 1. 初始化数据库
```bash
# 第一步：采集基础数据（最近1年）
python direct_hk_historical_collection.py
# 配置：collection_mode='all', 日期范围=最近1年

# 第二步：设置定时任务，每日同步
# 使用 manual_hk_historical_collection.py
```

### 2. 日常维护
```bash
# 每天收盘后运行
python manual_hk_historical_collection.py
# 配置：collection_mode='watchlist', 日期=当天
```

### 3. 数据补充
```bash
# 发现缺失数据时
python direct_hk_historical_collection.py
# 配置：指定缺失的日期范围和股票代码
```

---

## 注意事项

### manual_hk_historical_collection.py
1. ⚠️ 必须先有实时数据才能同步
2. ⚠️ 只能同步已采集的日期
3. ✅ 适合配合定时任务使用
4. ✅ 性能好，适合频繁运行

### direct_hk_historical_collection.py
1. ⚠️ 受API限流限制，采集速度慢
2. ⚠️ 需要稳定的网络连接
3. ✅ 可以采集任意历史日期
4. ✅ 适合一次性大批量采集

---

## 常见问题

### Q1: 应该使用哪个程序？
**A:** 
- 如果已有实时数据采集服务 → 使用 `manual_hk_historical_collection.py`
- 如果需要补充历史数据 → 使用 `direct_hk_historical_collection.py`
- 如果初次建立数据库 → 使用 `direct_hk_historical_collection.py`

### Q2: 两个程序可以同时运行吗？
**A:** 可以，但不建议。它们都会写入同一个数据库表，可能造成数据冲突。

### Q3: 如何提高采集速度？
**A:** 
- manual程序：已经很快，无需优化
- direct程序：可以分批采集，多次运行

### Q4: 指标计算失败怎么办？
**A:** 
- 检查是否有足够的历史数据
- 查看日志文件了解具体错误
- 可以单独运行指标计算程序

---

## 文件清单

1. **manual_hk_historical_collection.py** - 基于实时数据的采集程序
2. **direct_hk_historical_collection.py** - 直接从API采集的程序
3. **manual_hk_historical_collection_README.md** - 详细使用说明
4. **本文档** - 程序对比说明

---

## 技术支持

如遇到问题：
1. 查看对应的日志文件
2. 检查数据库连接和表结构
3. 确认网络连接（direct程序）
4. 验证实时数据是否存在（manual程序）
5. 查看程序代码注释和文档

---

## 总结

- **manual_hk_historical_collection.py**：快速、稳定，适合日常使用
- **direct_hk_historical_collection.py**：灵活、独立，适合初始化和补数据

根据您的实际需求选择合适的程序，或者两者配合使用以达到最佳效果。
