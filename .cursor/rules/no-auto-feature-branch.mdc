---
description: 未经用户明确要求，不要为每次修改新建 git 功能分支
alwaysApply: true
---

# Git：不要默认新建功能分支

除非用户**明确要求**「新建分支 / 开 PR 分支 / checkout -b」等，否则：

1. 在用户当前分支上直接修改（常见为 `main`）。
2. 不要为每个小修复或功能自动 `git switch -c cursor/...`。
3. 用户要求提交时，在当前分支提交；用户要求推送/开 PR 时再按其指示操作。

云端 Agent 等环境若系统强制使用隔离分支，应在回复中说明，并仍尽量避免额外再拆新分支。
