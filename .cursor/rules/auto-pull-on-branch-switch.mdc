---
description: 切换分支后自动 git pull 同步远程
alwaysApply: true
---

# 切换分支后自动拉取

当用户要求切换到某个 git 分支（或检测到需要切换工作分支）时：

1. 执行 `git switch <branch>`（或 `git checkout`）切换分支
2. **立刻**执行 `git pull`（可快进则快进；有冲突则停下来说明，不要强推或硬重置）
3. 用 `SetActiveBranch`（如可用）同步 Cursor 当前分支元数据
4. 简要告知：当前分支、是否已与远程同步、若有本地未提交改动则先说明再决定如何处理

不要等用户再说「需要 pull」；切换分支即默认拉取。
