---
description: 项目全局约定（测试目录、文档目录、语言、数据库、开发流程）
alwaysApply: true
---

test程序放在.\test目录下。
word、ppt相关文件放在.\exported_docs目录下。
总是用中文回答。
本系统使用 **PostgreSQL** 数据库（生产与日常开发环境）。编写 SQL、迁移脚本、索引与查询优化时，应默认按 PostgreSQL 语法与特性实现（如 `DISTINCT ON`、`LATERAL`、`IF NOT EXISTS`、复合索引等）。SQLite 仅用于部分单元测试的内存库，不作为生产环境假设。
修改程序时，请通读本功能相关前后台代码、数据模型、业务流程和规则，认真理解后再实现。
