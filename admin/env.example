# 本地直连后端（npm run dev 也可用相对路径 /api/admin，经 Vite 代理到 localhost:5000）
VITE_API_BASE_URL=http://localhost:5000/api/admin
# 生产构建部署到与 API 同域时，使用相对路径（或与 Nginx 中 location 一致的前缀）：
# VITE_API_BASE_URL=/api/admin